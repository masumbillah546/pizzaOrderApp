import {View, Text} from 'react-native';

const UpcomingScreen = () => (
  <View
    style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    }}>
    <Text style={{fontSize: 20, color: '#5C5C5C'}}>Page upcoming..</Text>
  </View>
);
export default UpcomingScreen;
